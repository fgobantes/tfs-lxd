nix-shell -p lxd-lts
sudo lxd
sudo lxc launch ubuntu:24.04 "${VM_NAME}" --vm    -c limits.cpu=4     -c limits.memory=8GiB     -c security.secureboot=false    --device root,size=100GiB 
Creating tfs-vm
Starting tfs-vm                             
Error: Failed to add CPUs: Failed to add device: Invalid parameter type for 'core-id', expected: integer
Try `lxc info --show-log local:tfs-vm` for more info



# --device root,size=100GiB


  # -c limits.cpu=4 \
  # ${USE_SUDO} lxc init ubuntu:24.04 "${VM_NAME}" --vm \
  #   -c limits.memory=8GiB \
  #   -c security.secureboot=false

  # ${USE_SUDO} lxc config device add "${VM_NAME}" root disk pool=default source=default
  # ${USE_SUDO} lxc config device add "${VM_NAME}" eth0 nic network=lxdbr0 boot.index=0

  # ${USE_SUDO} lxc start "${VM_NAME}"
